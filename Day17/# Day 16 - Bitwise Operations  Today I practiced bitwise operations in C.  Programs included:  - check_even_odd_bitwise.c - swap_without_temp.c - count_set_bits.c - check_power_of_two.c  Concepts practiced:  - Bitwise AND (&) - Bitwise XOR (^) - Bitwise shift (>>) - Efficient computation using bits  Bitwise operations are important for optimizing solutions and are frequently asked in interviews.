#include <stdio.h>

struct Student {
    int roll;
    char name[50];
    float marks;
};

int main() {
    FILE *fp;
    struct Student s;
    int n;

    fp = fopen("students.txt", "w");

    printf("Enter number of students: ");
    scanf("%d", &n);
